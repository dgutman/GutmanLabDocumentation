import { c as createComponent, r as renderComponent, a as renderTemplate, m as maybeRenderHead, F as Fragment, d as defineScriptVars } from '../../chunks/astro/server_Dpy5uBJK.mjs';
import 'piccolore';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';
import { $ as $$Layout } from '../../chunks/Layout_C5_pddvX.mjs';
import { l as loadPubMedCache } from '../../chunks/pubmed_BEMHzYvi.mjs';
/* empty css                                            */
export { renderers } from '../../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$PubmedReview = createComponent(($$result, $$props, $$slots) => {
  function loadApprovedFromConfig() {
    try {
      const p = join(
        process.cwd(),
        "content/publications/pubmed-config.json"
      );
      const j = JSON.parse(readFileSync(p, "utf-8"));
      if (!Array.isArray(j.approvedPmids)) return [];
      return j.approvedPmids.map((x) => String(x));
    } catch {
      return [];
    }
  }
  const approvedFromConfig = loadApprovedFromConfig();
  const cache = loadPubMedCache();
  const cacheData = cache ? {
    articles: cache.articles,
    excludedPmids: cache.excludedPmids ?? [],
    query: cache.query,
    generatedAt: cache.generatedAt,
    pmidCount: cache.pmidCount
  } : null;
  return renderTemplate`${renderComponent($$result, "Layout", $$Layout, { "title": "PubMed bibliography (curate)", "description": "Review PubMed search results, exclude mis-attributed papers, export for pubmed-config.json.", "noIndex": true, "data-astro-cid-a7warg3d": true }, { "default": ($$result2) => renderTemplate` ${maybeRenderHead()}<h1 class="page-title" data-astro-cid-a7warg3d>PubMed bibliography</h1> <p class="review-lede" data-astro-cid-a7warg3d>
All hits from your saved PubMed query. <strong data-astro-cid-a7warg3d>Approve</strong> papers you
    have checked (yours or OK to ignore); after each scrape, only new PMIDs stay
    under “Needs review.” Use <strong data-astro-cid-a7warg3d>Exclude</strong> for mis-attributed
    papers. Use the <strong data-astro-cid-a7warg3d>Export</strong> buttons below to copy
<strong data-astro-cid-a7warg3d>denied</strong> (excluded) and/or <strong data-astro-cid-a7warg3d>approved</strong> PMID
    lists; denied PMIDs go into <code data-astro-cid-a7warg3d>excludedPmids</code>, approved into
<code data-astro-cid-a7warg3d>approvedPmids</code> in <code data-astro-cid-a7warg3d>pubmed-config.json</code> (this repo
    already seeds <code data-astro-cid-a7warg3d>approvedPmids</code>; add more after export + rebuild).
</p> ${!cacheData ? renderTemplate`<p class="placeholder" data-astro-cid-a7warg3d>
No PubMed cache yet. Set your email in${" "} <code data-astro-cid-a7warg3d>content/publications/pubmed-config.json</code> and run${" "} <code data-astro-cid-a7warg3d>npm run sync:pubmed</code>.
</p>` : renderTemplate`${renderComponent($$result2, "Fragment", Fragment, { "data-astro-cid-a7warg3d": true }, { "default": ($$result3) => renderTemplate` <div class="review-meta" data-astro-cid-a7warg3d> ${cacheData.generatedAt ? renderTemplate`<span data-astro-cid-a7warg3d>Last synced: ${cacheData.generatedAt}</span>` : null} ${cacheData.query ? renderTemplate`<span data-astro-cid-a7warg3d>
· Query: <code data-astro-cid-a7warg3d>${cacheData.query}</code> </span>` : null} ${cacheData.pmidCount != null ? renderTemplate`<span data-astro-cid-a7warg3d> · ${cacheData.pmidCount} PMIDs</span>` : null} </div> <div class="review-export-panel" aria-labelledby="export-heading" data-astro-cid-a7warg3d> <h2 id="export-heading" class="review-export-panel__title" data-astro-cid-a7warg3d>
Export PMID lists
</h2> <p class="review-export-panel__desc" data-astro-cid-a7warg3d> <strong data-astro-cid-a7warg3d>Denied</strong> = excluded (not your papers).${" "} <strong data-astro-cid-a7warg3d>Approved</strong> = you’ve reviewed and confirmed. Each
            button copies JSON to the clipboard.
</p> <div class="review-toolbar review-toolbar--export" data-astro-cid-a7warg3d> <button type="button" class="btn btn--primary" id="btn-export-denied" data-astro-cid-a7warg3d>
Export denied (excluded)
</button> <button type="button" class="btn btn--primary btn--secondary" id="btn-export-approved" data-astro-cid-a7warg3d>
Export approved
</button> <button type="button" class="btn btn--ghost" id="btn-export-both" data-astro-cid-a7warg3d>
Export both (one JSON file)
</button> </div> <p class="review-export-hint" id="export-hint" hidden data-astro-cid-a7warg3d>
Copied denied PMIDs. Paste into <code data-astro-cid-a7warg3d>excludedPmids</code> in
            pubmed-config.json, then run <code data-astro-cid-a7warg3d>npm run sync:pubmed</code>.
</p> <p class="review-export-hint" id="export-approved-hint" hidden data-astro-cid-a7warg3d>
Copied merged approved PMIDs (config + browser). Merge into
            approvedPmids in pubmed-config.json if you want them in git.
</p> <p class="review-export-hint" id="export-both-hint" hidden data-astro-cid-a7warg3d>
Copied combined snapshot (<code data-astro-cid-a7warg3d>excludedPmids</code> +${" "} <code data-astro-cid-a7warg3d>approvedPmids</code>).
</p> </div> <div class="review-toolbar review-toolbar--danger" data-astro-cid-a7warg3d> <button type="button" class="btn btn--ghost" id="btn-clear-local" data-astro-cid-a7warg3d>
Clear browser-only denied
</button> <button type="button" class="btn btn--ghost" id="btn-clear-approved" data-astro-cid-a7warg3d>
Clear all approvals
</button> </div> <div id="review-root" class="review-root" data-astro-cid-a7warg3d></div> ` })}`}${cacheData ? renderTemplate(_a || (_a = __template(["<script>(function(){", '\n        (function () {\n          const STORAGE_EXCLUDED = "gutman-pubmed-local-excluded";\n          const STORAGE_APPROVED = "gutman-pubmed-approved-pmids";\n          const data = cacheData;\n          const approvedFromConfigSet = new Set(\n            (approvedFromConfig || []).map(String),\n          );\n\n          function getLocalExcluded() {\n            try {\n              const a = JSON.parse(\n                localStorage.getItem(STORAGE_EXCLUDED) || "[]",\n              );\n              return Array.isArray(a) ? a.map(String) : [];\n            } catch {\n              return [];\n            }\n          }\n\n          function setLocalExcluded(ids) {\n            localStorage.setItem(STORAGE_EXCLUDED, JSON.stringify(ids));\n          }\n\n          function getLocalApproved() {\n            try {\n              const a = JSON.parse(\n                localStorage.getItem(STORAGE_APPROVED) || "[]",\n              );\n              return Array.isArray(a) ? a.map(String) : [];\n            } catch {\n              return [];\n            }\n          }\n\n          function setLocalApproved(ids) {\n            localStorage.setItem(STORAGE_APPROVED, JSON.stringify(ids));\n          }\n\n          function mergedApprovedSet() {\n            return new Set([\n              ...approvedFromConfigSet,\n              ...getLocalApproved(),\n            ]);\n          }\n\n          function unapprove(pmid) {\n            if (approvedFromConfigSet.has(pmid)) {\n              alert(\n                "This PMID is listed in approvedPmids in pubmed-config.json. Remove it there and rebuild, or export and edit the JSON.",\n              );\n              return;\n            }\n            setLocalApproved(getLocalApproved().filter((x) => x !== pmid));\n          }\n\n          function removeFromLocalApprovedOnly(pmid) {\n            setLocalApproved(getLocalApproved().filter((x) => x !== pmid));\n          }\n\n          function mergedExcluded() {\n            const cfg = new Set((data.excludedPmids || []).map(String));\n            const loc = new Set(getLocalExcluded());\n            return { cfg, loc, merged: new Set([...cfg, ...loc]) };\n          }\n\n          function sortByYearDesc(list) {\n            return [...list].sort(\n              (x, y) =>\n                (y.year || 0) - (x.year || 0) ||\n                (x.title || "").localeCompare(y.title || ""),\n            );\n          }\n\n          function escapeHtml(s) {\n            const d = document.createElement("div");\n            d.textContent = s;\n            return d.innerHTML;\n          }\n\n          function paperCard(a, opts) {\n            const { excluded, reason, loc, approved } = opts;\n            const pmid = String(a.pmid);\n            const types =\n              a.publicationTypes && a.publicationTypes.length\n                ? `<p class="pubmed-paper__types">${escapeHtml(a.publicationTypes.join(" \xB7 "))}</p>`\n                : "";\n            const abs = a.abstract\n              ? `<details class="pubmed-paper__abstract"><summary>Abstract</summary><p>${escapeHtml(a.abstract)}</p></details>`\n              : "";\n            let badge = "";\n            if (excluded) {\n              badge = `<span class="review-badge review-badge--excluded">Excluded${reason ? " \xB7 " + escapeHtml(reason) : ""}</span>`;\n            } else if (approved) {\n              badge = `<span class="review-badge review-badge--ok">Reviewed</span>`;\n            } else {\n              badge = `<span class="review-badge review-badge--pending">Needs review</span>`;\n            }\n            const restoreSource =\n              loc && loc.has(pmid) ? "local" : "config";\n            let actions = "";\n            if (excluded) {\n              actions = `<button type="button" class="btn btn--small btn--restore" data-pmid="${escapeHtml(pmid)}" data-source="${restoreSource}">Restore</button>`;\n            } else {\n              const approveBtn = approved\n                ? `<button type="button" class="btn btn--small btn--unapprove" data-pmid="${escapeHtml(pmid)}">Unapprove</button>`\n                : `<button type="button" class="btn btn--small btn--approve" data-pmid="${escapeHtml(pmid)}">Approve</button>`;\n              actions = `${approveBtn}<button type="button" class="btn btn--small btn--exclude" data-pmid="${escapeHtml(pmid)}">Exclude</button>`;\n            }\n            const cardClass = [\n              "review-card",\n              excluded ? "review-card--excluded" : "",\n              !excluded && !approved ? "review-card--pending" : "",\n            ]\n              .filter(Boolean)\n              .join(" ");\n            return `<article class="${cardClass}" data-pmid="${escapeHtml(pmid)}">\n            <div class="review-card__head">\n              ${badge}\n              ${actions}\n            </div>\n            <h3 class="pubmed-paper__title"><a href="${escapeHtml(a.pubmedUrl)}">${escapeHtml(a.title)}</a></h3>\n            <p class="pubmed-paper__authors">${escapeHtml(a.authors)}</p>\n            <p class="pubmed-paper__journal">${escapeHtml(a.journal)}${a.year ? " \xB7 " + a.year : ""}${a.pubMonth ? " \xB7 " + escapeHtml(a.pubMonth) : ""}</p>\n            ${types}\n            ${abs}\n          </article>`;\n          }\n\n          function render() {\n            const root = document.getElementById("review-root");\n            if (!root || !data.articles) return;\n            const approvedSet = mergedApprovedSet();\n            const { cfg, loc, merged } = mergedExcluded();\n            const active = [];\n            const excluded = [];\n            for (const a of data.articles) {\n              const pmid = String(a.pmid);\n              if (merged.has(pmid)) {\n                let reason = "browser";\n                if (cfg.has(pmid) && loc.has(pmid)) reason = "config + browser";\n                else if (cfg.has(pmid)) reason = "config";\n                excluded.push({ a, reason });\n              } else active.push(a);\n            }\n            const needsReview = sortByYearDesc(\n              active.filter((a) => !approvedSet.has(String(a.pmid))),\n            );\n            const reviewed = sortByYearDesc(\n              active.filter((a) => approvedSet.has(String(a.pmid))),\n            );\n\n            let html = `<section class="review-section" aria-labelledby="pending-h"><h2 id="pending-h" class="review-section__title">Needs review (${needsReview.length})</h2><p class="review-section__sub">New since last scrape or not yet approved.</p><div class="review-stack">`;\n            for (const a of needsReview) {\n              html += paperCard(a, {\n                excluded: false,\n                loc,\n                approved: false,\n              });\n            }\n            if (!needsReview.length) {\n              html += `<p class="review-empty">Nothing pending \u2014 you\u2019re caught up.</p>`;\n            }\n            html += `</div></section><section class="review-section" aria-labelledby="ok-h"><h2 id="ok-h" class="review-section__title">Reviewed (${reviewed.length})</h2><p class="review-section__sub">Merged from pubmed-config.json approvedPmids plus any you approved in this browser.</p><div class="review-stack">`;\n            for (const a of reviewed) {\n              html += paperCard(a, {\n                excluded: false,\n                loc,\n                approved: true,\n              });\n            }\n            if (!reviewed.length) {\n              html += `<p class="review-empty">No approvals yet. Use Approve on papers you\u2019ve checked.</p>`;\n            }\n            html += `</div></section><section class="review-section review-section--muted" aria-labelledby="ex-h"><h2 id="ex-h" class="review-section__title">Excluded (${excluded.length})</h2><div class="review-stack">`;\n            for (const { a, reason } of excluded) {\n              html += paperCard(a, {\n                excluded: true,\n                reason: reason,\n                loc: loc,\n              });\n            }\n            html += "</div></section>";\n            root.innerHTML = html;\n\n            root.querySelectorAll(".btn--approve").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                if (!pmid) return;\n                if (mergedApprovedSet().has(pmid)) return;\n                const cur = getLocalApproved();\n                cur.push(pmid);\n                setLocalApproved(cur);\n                render();\n              });\n            });\n            root.querySelectorAll(".btn--unapprove").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                if (!pmid) return;\n                unapprove(pmid);\n                render();\n              });\n            });\n            root.querySelectorAll(".btn--exclude").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                if (!pmid) return;\n                removeFromLocalApprovedOnly(pmid);\n                const cur = getLocalExcluded();\n                if (!cur.includes(pmid)) {\n                  cur.push(pmid);\n                  setLocalExcluded(cur);\n                }\n                render();\n              });\n            });\n            root.querySelectorAll(".btn--restore").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                const source = btn.getAttribute("data-source");\n                if (!pmid) return;\n                if (source === "config") {\n                  alert(\n                    "This PMID is listed in pubmed-config.json. Remove it from excludedPmids there, then run sync.",\n                  );\n                  return;\n                }\n                setLocalExcluded(getLocalExcluded().filter((x) => x !== pmid));\n                render();\n              });\n            });\n          }\n\n          function copyText(text, hintId) {\n            ["export-hint", "export-approved-hint", "export-both-hint"].forEach(\n              (id) => {\n                const el = document.getElementById(id);\n                if (el) el.hidden = true;\n              },\n            );\n            const done = () => {\n              const h = document.getElementById(hintId);\n              if (h) {\n                h.hidden = false;\n                setTimeout(() => {\n                  h.hidden = true;\n                }, 5000);\n              }\n            };\n            if (navigator.clipboard && navigator.clipboard.writeText) {\n              navigator.clipboard.writeText(text).then(done).catch(() => {\n                window.prompt("Copy:", text);\n                done();\n              });\n            } else {\n              window.prompt("Copy:", text);\n              done();\n            }\n          }\n\n          document.getElementById("btn-export-denied")?.addEventListener("click", () => {\n            const { merged } = mergedExcluded();\n            const arr = [...merged].sort();\n            copyText(JSON.stringify(arr, null, 2), "export-hint");\n          });\n\n          document.getElementById("btn-export-approved")?.addEventListener("click", () => {\n            const approved = [...mergedApprovedSet()].sort();\n            copyText(JSON.stringify(approved, null, 2), "export-approved-hint");\n          });\n\n          document.getElementById("btn-export-both")?.addEventListener("click", () => {\n            const { merged } = mergedExcluded();\n            const snapshot = {\n              excludedPmids: [...merged].sort(),\n              approvedPmids: [...mergedApprovedSet()].sort(),\n            };\n            copyText(JSON.stringify(snapshot, null, 2), "export-both-hint");\n          });\n\n          document.getElementById("btn-clear-local")?.addEventListener("click", () => {\n            localStorage.removeItem(STORAGE_EXCLUDED);\n            render();\n          });\n\n          document.getElementById("btn-clear-approved")?.addEventListener("click", () => {\n            if (\n              !confirm(\n                "Clear browser-only approvals? PMIDs in pubmed-config.json approvedPmids stay until you edit that file.",\n              )\n            ) {\n              return;\n            }\n            localStorage.removeItem(STORAGE_APPROVED);\n            render();\n          });\n\n          render();\n        })();\n      })();<\/script>'], ["<script>(function(){", '\n        (function () {\n          const STORAGE_EXCLUDED = "gutman-pubmed-local-excluded";\n          const STORAGE_APPROVED = "gutman-pubmed-approved-pmids";\n          const data = cacheData;\n          const approvedFromConfigSet = new Set(\n            (approvedFromConfig || []).map(String),\n          );\n\n          function getLocalExcluded() {\n            try {\n              const a = JSON.parse(\n                localStorage.getItem(STORAGE_EXCLUDED) || "[]",\n              );\n              return Array.isArray(a) ? a.map(String) : [];\n            } catch {\n              return [];\n            }\n          }\n\n          function setLocalExcluded(ids) {\n            localStorage.setItem(STORAGE_EXCLUDED, JSON.stringify(ids));\n          }\n\n          function getLocalApproved() {\n            try {\n              const a = JSON.parse(\n                localStorage.getItem(STORAGE_APPROVED) || "[]",\n              );\n              return Array.isArray(a) ? a.map(String) : [];\n            } catch {\n              return [];\n            }\n          }\n\n          function setLocalApproved(ids) {\n            localStorage.setItem(STORAGE_APPROVED, JSON.stringify(ids));\n          }\n\n          function mergedApprovedSet() {\n            return new Set([\n              ...approvedFromConfigSet,\n              ...getLocalApproved(),\n            ]);\n          }\n\n          function unapprove(pmid) {\n            if (approvedFromConfigSet.has(pmid)) {\n              alert(\n                "This PMID is listed in approvedPmids in pubmed-config.json. Remove it there and rebuild, or export and edit the JSON.",\n              );\n              return;\n            }\n            setLocalApproved(getLocalApproved().filter((x) => x !== pmid));\n          }\n\n          function removeFromLocalApprovedOnly(pmid) {\n            setLocalApproved(getLocalApproved().filter((x) => x !== pmid));\n          }\n\n          function mergedExcluded() {\n            const cfg = new Set((data.excludedPmids || []).map(String));\n            const loc = new Set(getLocalExcluded());\n            return { cfg, loc, merged: new Set([...cfg, ...loc]) };\n          }\n\n          function sortByYearDesc(list) {\n            return [...list].sort(\n              (x, y) =>\n                (y.year || 0) - (x.year || 0) ||\n                (x.title || "").localeCompare(y.title || ""),\n            );\n          }\n\n          function escapeHtml(s) {\n            const d = document.createElement("div");\n            d.textContent = s;\n            return d.innerHTML;\n          }\n\n          function paperCard(a, opts) {\n            const { excluded, reason, loc, approved } = opts;\n            const pmid = String(a.pmid);\n            const types =\n              a.publicationTypes && a.publicationTypes.length\n                ? \\`<p class="pubmed-paper__types">\\${escapeHtml(a.publicationTypes.join(" \xB7 "))}</p>\\`\n                : "";\n            const abs = a.abstract\n              ? \\`<details class="pubmed-paper__abstract"><summary>Abstract</summary><p>\\${escapeHtml(a.abstract)}</p></details>\\`\n              : "";\n            let badge = "";\n            if (excluded) {\n              badge = \\`<span class="review-badge review-badge--excluded">Excluded\\${reason ? " \xB7 " + escapeHtml(reason) : ""}</span>\\`;\n            } else if (approved) {\n              badge = \\`<span class="review-badge review-badge--ok">Reviewed</span>\\`;\n            } else {\n              badge = \\`<span class="review-badge review-badge--pending">Needs review</span>\\`;\n            }\n            const restoreSource =\n              loc && loc.has(pmid) ? "local" : "config";\n            let actions = "";\n            if (excluded) {\n              actions = \\`<button type="button" class="btn btn--small btn--restore" data-pmid="\\${escapeHtml(pmid)}" data-source="\\${restoreSource}">Restore</button>\\`;\n            } else {\n              const approveBtn = approved\n                ? \\`<button type="button" class="btn btn--small btn--unapprove" data-pmid="\\${escapeHtml(pmid)}">Unapprove</button>\\`\n                : \\`<button type="button" class="btn btn--small btn--approve" data-pmid="\\${escapeHtml(pmid)}">Approve</button>\\`;\n              actions = \\`\\${approveBtn}<button type="button" class="btn btn--small btn--exclude" data-pmid="\\${escapeHtml(pmid)}">Exclude</button>\\`;\n            }\n            const cardClass = [\n              "review-card",\n              excluded ? "review-card--excluded" : "",\n              !excluded && !approved ? "review-card--pending" : "",\n            ]\n              .filter(Boolean)\n              .join(" ");\n            return \\`<article class="\\${cardClass}" data-pmid="\\${escapeHtml(pmid)}">\n            <div class="review-card__head">\n              \\${badge}\n              \\${actions}\n            </div>\n            <h3 class="pubmed-paper__title"><a href="\\${escapeHtml(a.pubmedUrl)}">\\${escapeHtml(a.title)}</a></h3>\n            <p class="pubmed-paper__authors">\\${escapeHtml(a.authors)}</p>\n            <p class="pubmed-paper__journal">\\${escapeHtml(a.journal)}\\${a.year ? " \xB7 " + a.year : ""}\\${a.pubMonth ? " \xB7 " + escapeHtml(a.pubMonth) : ""}</p>\n            \\${types}\n            \\${abs}\n          </article>\\`;\n          }\n\n          function render() {\n            const root = document.getElementById("review-root");\n            if (!root || !data.articles) return;\n            const approvedSet = mergedApprovedSet();\n            const { cfg, loc, merged } = mergedExcluded();\n            const active = [];\n            const excluded = [];\n            for (const a of data.articles) {\n              const pmid = String(a.pmid);\n              if (merged.has(pmid)) {\n                let reason = "browser";\n                if (cfg.has(pmid) && loc.has(pmid)) reason = "config + browser";\n                else if (cfg.has(pmid)) reason = "config";\n                excluded.push({ a, reason });\n              } else active.push(a);\n            }\n            const needsReview = sortByYearDesc(\n              active.filter((a) => !approvedSet.has(String(a.pmid))),\n            );\n            const reviewed = sortByYearDesc(\n              active.filter((a) => approvedSet.has(String(a.pmid))),\n            );\n\n            let html = \\`<section class="review-section" aria-labelledby="pending-h"><h2 id="pending-h" class="review-section__title">Needs review (\\${needsReview.length})</h2><p class="review-section__sub">New since last scrape or not yet approved.</p><div class="review-stack">\\`;\n            for (const a of needsReview) {\n              html += paperCard(a, {\n                excluded: false,\n                loc,\n                approved: false,\n              });\n            }\n            if (!needsReview.length) {\n              html += \\`<p class="review-empty">Nothing pending \u2014 you\u2019re caught up.</p>\\`;\n            }\n            html += \\`</div></section><section class="review-section" aria-labelledby="ok-h"><h2 id="ok-h" class="review-section__title">Reviewed (\\${reviewed.length})</h2><p class="review-section__sub">Merged from pubmed-config.json approvedPmids plus any you approved in this browser.</p><div class="review-stack">\\`;\n            for (const a of reviewed) {\n              html += paperCard(a, {\n                excluded: false,\n                loc,\n                approved: true,\n              });\n            }\n            if (!reviewed.length) {\n              html += \\`<p class="review-empty">No approvals yet. Use Approve on papers you\u2019ve checked.</p>\\`;\n            }\n            html += \\`</div></section><section class="review-section review-section--muted" aria-labelledby="ex-h"><h2 id="ex-h" class="review-section__title">Excluded (\\${excluded.length})</h2><div class="review-stack">\\`;\n            for (const { a, reason } of excluded) {\n              html += paperCard(a, {\n                excluded: true,\n                reason: reason,\n                loc: loc,\n              });\n            }\n            html += "</div></section>";\n            root.innerHTML = html;\n\n            root.querySelectorAll(".btn--approve").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                if (!pmid) return;\n                if (mergedApprovedSet().has(pmid)) return;\n                const cur = getLocalApproved();\n                cur.push(pmid);\n                setLocalApproved(cur);\n                render();\n              });\n            });\n            root.querySelectorAll(".btn--unapprove").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                if (!pmid) return;\n                unapprove(pmid);\n                render();\n              });\n            });\n            root.querySelectorAll(".btn--exclude").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                if (!pmid) return;\n                removeFromLocalApprovedOnly(pmid);\n                const cur = getLocalExcluded();\n                if (!cur.includes(pmid)) {\n                  cur.push(pmid);\n                  setLocalExcluded(cur);\n                }\n                render();\n              });\n            });\n            root.querySelectorAll(".btn--restore").forEach((btn) => {\n              btn.addEventListener("click", () => {\n                const pmid = btn.getAttribute("data-pmid");\n                const source = btn.getAttribute("data-source");\n                if (!pmid) return;\n                if (source === "config") {\n                  alert(\n                    "This PMID is listed in pubmed-config.json. Remove it from excludedPmids there, then run sync.",\n                  );\n                  return;\n                }\n                setLocalExcluded(getLocalExcluded().filter((x) => x !== pmid));\n                render();\n              });\n            });\n          }\n\n          function copyText(text, hintId) {\n            ["export-hint", "export-approved-hint", "export-both-hint"].forEach(\n              (id) => {\n                const el = document.getElementById(id);\n                if (el) el.hidden = true;\n              },\n            );\n            const done = () => {\n              const h = document.getElementById(hintId);\n              if (h) {\n                h.hidden = false;\n                setTimeout(() => {\n                  h.hidden = true;\n                }, 5000);\n              }\n            };\n            if (navigator.clipboard && navigator.clipboard.writeText) {\n              navigator.clipboard.writeText(text).then(done).catch(() => {\n                window.prompt("Copy:", text);\n                done();\n              });\n            } else {\n              window.prompt("Copy:", text);\n              done();\n            }\n          }\n\n          document.getElementById("btn-export-denied")?.addEventListener("click", () => {\n            const { merged } = mergedExcluded();\n            const arr = [...merged].sort();\n            copyText(JSON.stringify(arr, null, 2), "export-hint");\n          });\n\n          document.getElementById("btn-export-approved")?.addEventListener("click", () => {\n            const approved = [...mergedApprovedSet()].sort();\n            copyText(JSON.stringify(approved, null, 2), "export-approved-hint");\n          });\n\n          document.getElementById("btn-export-both")?.addEventListener("click", () => {\n            const { merged } = mergedExcluded();\n            const snapshot = {\n              excludedPmids: [...merged].sort(),\n              approvedPmids: [...mergedApprovedSet()].sort(),\n            };\n            copyText(JSON.stringify(snapshot, null, 2), "export-both-hint");\n          });\n\n          document.getElementById("btn-clear-local")?.addEventListener("click", () => {\n            localStorage.removeItem(STORAGE_EXCLUDED);\n            render();\n          });\n\n          document.getElementById("btn-clear-approved")?.addEventListener("click", () => {\n            if (\n              !confirm(\n                "Clear browser-only approvals? PMIDs in pubmed-config.json approvedPmids stay until you edit that file.",\n              )\n            ) {\n              return;\n            }\n            localStorage.removeItem(STORAGE_APPROVED);\n            render();\n          });\n\n          render();\n        })();\n      })();<\/script>'])), defineScriptVars({ cacheData, approvedFromConfig })) : null}` })} `;
}, "/tmp/GutmanLabDocumentation/src/pages/publications/pubmed-review.astro", void 0);

const $$file = "/tmp/GutmanLabDocumentation/src/pages/publications/pubmed-review.astro";
const $$url = "/GutmanLabDocumentation/publications/pubmed-review/";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$PubmedReview,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
