---
// PapersList — server-side rendered from a prop.
// Large JSON data is loaded in the parent to avoid esbuild issues.
interface Props {
  papersHtml: string;
  paperCount: number;
}

const { papersHtml, paperCount } = Astro.props;
---

<slot />

<script>
  // Render placeholder; the actual list is server-rendered by the parent.
  // This component just provides the prop interface for consistency.
</script>
